import { DayData } from '../types';

// Sample/bundled data — intentionally empty.
// All data is fetched LIVE from public/data/*.json files.
// Just drop your DD-MM-YYYY.json export into public/data/ and it will
// be auto-discovered and displayed — no code changes needed.
export const sampleData: Record<string, DayData> = {};
